
function smallestCommon(arr) {
  let numTop = highest integer;
  let numBottom = smallest integer;

  arr.sort();
  
}
// range between two given numbers

// sort the numbers first


let sortedArray = 


// make multiples between all integers in the range

//
function arrayMaker() {
  
}

let array1 = Array.from(Array(numTop), (x, index) => index + 1);

// loop through the numbers

// set common multiples


// search through those multiles one the is common between both numbers